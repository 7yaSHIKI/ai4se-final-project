# SPEC.md - AI-Powered RSS Aggregator

> **项目名称**：AI-Powered RSS Aggregator (RSS 聚合器 + AI 摘要)  
> **版本**：1.0  
> **最后更新**：2026-08-13  
> **作者**：7yaSHIKI  
> **状态**：设计阶段

---

## 1. 问题陈述

### 1.1 要解决什么问题？

现代互联网用户面临严重的**信息过载**问题：

- **分散性**：优质内容分散在多个博客、新闻网站、技术社区
- **低效性**：用户需要打开 10+ 个网站/APP 才能获取每日资讯
- **时间成本高**：长文章需要大量时间阅读，难以快速筛选
- **体验不一致**：每个网站界面不同，阅读体验割裂

### 1.2 目标用户

**主要用户**：个人用户（开发者、技术爱好者、终身学习者）

**典型画像**：
- 关注 5-20 个优质信息源（技术博客、新闻网站）
- 每天早上花 15-30 分钟浏览资讯
- 希望快速了解内容要点，决定是否深入阅读
- 追求高效的信息获取方式

### 1.3 为什么值得做？

1. **真实需求**：信息爆炸时代的刚需
2. **技术可行**：RSS 协议成熟，LLM API 稳定
3. **用户价值**：
   - 节省时间：从 1 小时缩短到 20 分钟
   - 提升效率：AI 摘要帮助快速筛选
   - 统一体验：一个界面看所有内容

---

## 2. 用户故事

遵循 INVEST 原则（Independent, Negotiable, Valuable, Estimable, Small, Testable）

### US-1: 订阅 RSS 源
**作为**用户  
**我想**添加我关注的博客/网站的 RSS 订阅源  
**以便**在一个地方查看所有更新

**验收标准**：
- 支持手动输入 RSS URL
- 支持输入网站 URL 自动检测 RSS
- 订阅成功后显示在订阅列表
- 无效 URL 显示错误提示

### US-2: 管理订阅源标签
**作为**用户  
**我想**给订阅源打上分类标签（如"技术"、"AI"、"前端"）  
**以便**按类别筛选和组织内容

**验收标准**：
- 添加订阅时可以设置标签
- 可以编辑已有订阅的标签
- 支持多标签（一个订阅源可以有多个标签）
- 标签可自定义输入

### US-3: 查看 AI 摘要
**作为**用户  
**我想**看到每篇文章的 AI 生成摘要（100-200字）  
**以便**快速了解文章内容，决定是否阅读原文

**验收标准**：
- 文章列表中每篇文章显示摘要
- 摘要保持原文语言（中文→中文，英文→英文）
- 摘要生成失败时显示"LLM 调用失败"并展示原文前 200 字
- 摘要生成中显示加载状态

### US-4: 按时间查看文章
**作为**用户  
**我想**按时间倒序查看所有文章（最新的在上面）  
**以便**快速浏览最新资讯

**验收标准**：
- 默认按发布时间倒序排列
- 显示文章发布时间（相对时间，如"2 小时前"）
- 只显示最近 7 天内的文章
- 超过 7 天的文章自动清理

### US-5: 按标签筛选文章
**作为**用户  
**我想**按订阅源标签筛选文章  
**以便**专注查看某个领域的内容

**验收标准**：
- 顶部显示所有标签选项
- 点击标签只显示该标签下订阅源的文章
- 支持"全部"选项显示所有文章
- 当前选中的标签高亮显示

### US-6: 跳转到原文
**作为**用户  
**我想**点击文章标题直接跳转到原文网站  
**以便**阅读完整内容

**验收标准**：
- 点击标题在新标签页打开原文
- 链接可访问且正确
- 显示订阅源名称（来源）

---

## 3. 功能规约

### 3.1 模块 1：订阅管理

#### 3.1.1 添加订阅源

**输入**：
- RSS URL（必填）或网站 URL（自动检测）
- 订阅源名称（可选，默认从 RSS feed 提取）
- 标签（可选，多个标签用逗号分隔）

**行为**：
1. 验证 URL 格式
2. 如果是网站 URL，尝试自动发现 RSS（解析 `<link rel="alternate" type="application/rss+xml">`）
3. 访问 RSS URL，验证是否可访问
4. 解析 RSS，提取 feed 标题
5. 保存到数据库

**输出**：
- 成功：显示"订阅成功"，跳转到订阅列表
- 失败：显示具体错误信息（"无法访问 URL"、"不是有效的 RSS"、"URL 格式错误"）

**边界条件**：
- URL 为空：提示"请输入 URL"
- RSS 解析失败：提示"无法解析 RSS，请检查 URL"
- 重复订阅：提示"该订阅源已存在"
- 网络超时（10 秒）：提示"网络连接超时"

**错误处理**：
- 捕获所有异常，不崩溃
- 记录错误日志（包含 URL 和错误详情）

#### 3.1.2 查看订阅列表

**输入**：无

**行为**：
1. 从数据库查询所有订阅源
2. 按创建时间倒序排列
3. 显示每个订阅源的名称、标签、RSS URL

**输出**：订阅源列表，每行包含：
- 订阅源名称
- 标签（多个标签用徽章显示）
- RSS URL（可点击）
- 删除按钮

**边界条件**：
- 无订阅源：显示"暂无订阅，点击添加"

#### 3.1.3 删除订阅源

**输入**：订阅源 ID

**行为**：
1. 弹出确认对话框："确定删除该订阅源吗？"
2. 用户确认后，删除数据库中的订阅源记录
3. **级联删除**：删除该订阅源下的所有文章

**输出**：
- 成功：显示"删除成功"，刷新订阅列表
- 失败：显示"删除失败"

**边界条件**：
- 订阅源不存在：提示"订阅源不存在"

#### 3.1.4 编辑订阅源标签

**输入**：
- 订阅源 ID
- 新标签（多个用逗号分隔）

**行为**：
1. 更新数据库中的标签字段
2. 验证标签格式（不能包含特殊字符）

**输出**：
- 成功：显示"更新成功"
- 失败：显示错误信息

**边界条件**：
- 标签为空：清空标签
- 标签过长（>50 字符）：提示"标签过长"

---

### 3.2 模块 2：内容抓取与 AI 摘要

#### 3.2.1 抓取 RSS 内容

**触发时机**：
- 用户手动点击"刷新"按钮
- 后台定时任务（每 1 小时执行一次）

**输入**：订阅源列表

**行为**：
1. 遍历所有订阅源
2. 对每个订阅源：
   - 访问 RSS URL
   - 解析 RSS XML
   - 提取所有文章条目（entry）
   - 对每篇文章：
     - 提取：标题、链接、发布时间、内容/描述
     - 检查是否已存在（根据 link 去重）
     - 如果不存在，插入数据库
3. 记录抓取日志

**输出**：
- 成功：显示"抓取完成，新增 X 篇文章"
- 失败：显示"抓取失败：[错误信息]"

**边界条件**：
- 订阅源无新文章：跳过
- RSS 解析失败：记录错误，继续下一个订阅源
- 网络超时（10 秒）：记录错误，继续下一个
- 文章发布时间超过 7 天：不抓取

**错误处理**：
- 单个订阅源失败不影响其他订阅源
- 记录详细错误日志

#### 3.2.2 生成 AI 摘要

**触发时机**：
- 用户打开文章列表页面时，对未生成摘要的文章实时生成

**输入**：
- 文章内容（优先使用 `content`，否则使用 `description`）
- 文章语言（自动检测）

**行为**：
1. 检查文章是否已有摘要（`summary` 字段不为空且 `summary_status = 'success'`）
2. 如果没有摘要：
   - 构建 LLM prompt：
     ```
     请为以下文章生成一个简短摘要（100-200字），用[文章语言]：
     
     [文章内容]
     ```
   - 调用 OpenAI API（模型：gpt-4o-mini）
   - 设置重试机制：最多重试 3 次
   - 保存摘要到数据库
   - 更新 `summary_status` 为 `success`

**输出**：
- 成功：返回 100-200 字的摘要
- 失败：
  - 更新 `summary_status` 为 `failed`
  - 返回"LLM 调用失败" + 原文前 200 字

**边界条件**：
- 文章内容为空：使用标题生成摘要
- 文章内容过长（>10000 字符）：截取前 5000 字符
- API Key 未配置：提示"请配置 OpenAI API Key"
- API 限额超限：显示"API 限额已用完"

**错误处理**：
- 网络错误：重试 3 次，失败后返回原文前 200 字
- API 错误（401/429/500）：记录日志，返回原文前 200 字
- 超时（30 秒）：取消请求，返回原文前 200 字

#### 3.2.3 自动清理旧文章

**触发时机**：
- 后台定时任务（每天凌晨 3 点执行一次）

**输入**：无

**行为**：
1. 查询发布时间 > 7 天的文章
2. 批量删除

**输出**：
- 日志记录删除数量

**边界条件**：
- 无旧文章：跳过

---

### 3.3 模块 3：WebUI 展示

#### 3.3.1 首页 - 文章列表

**路由**：`/`

**输入**：
- 查询参数：`tag`（可选，按标签筛选）

**行为**：
1. 查询数据库：
   - 获取最近 7 天的文章
   - 如果有 `tag` 参数，筛选订阅源包含该标签的文章
   - 按发布时间倒序排列
2. 对每篇文章：
   - 如果没有摘要，触发生成（异步）
3. 渲染页面

**输出**：HTML 页面，包含：
- 顶部导航栏：
  - Logo/标题
  - 标签筛选按钮（全部、技术、AI 等）
  - 刷新按钮
  - 订阅管理链接
- 文章列表：
  - 每篇文章卡片：
    - 文章标题（链接到原文）
    - 订阅源名称（小标签）
    - 发布时间（相对时间）
    - AI 摘要（或加载中/失败提示）
    - 标签徽章

**边界条件**：
- 无文章：显示"暂无文章，请添加订阅源"
- 摘要生成中：显示加载动画
- 摘要失败：显示"LLM 调用失败" + 原文前 200 字

**响应式设计**：
- 桌面端：文章卡片 2-3 列布局
- 移动端：单列布局

#### 3.3.2 订阅管理页面

**路由**：`/feeds`

**输入**：无

**行为**：
1. 查询所有订阅源
2. 渲染订阅管理页面

**输出**：HTML 页面，包含：
- 添加订阅表单：
  - RSS URL / 网站 URL 输入框
  - 标签输入框（多个标签用逗号分隔）
  - 添加按钮
- 订阅列表：
  - 每个订阅源：
    - 名称
    - RSS URL
    - 标签（可编辑）
    - 删除按钮

**边界条件**：
- 无订阅源：显示空状态提示

#### 3.3.3 API 接口

**POST `/api/feeds`** - 添加订阅源
- 请求体：`{ "url": "...", "tags": "技术,AI" }`
- 响应：`{ "success": true, "message": "订阅成功" }`

**DELETE `/api/feeds/{id}`** - 删除订阅源
- 响应：`{ "success": true }`

**POST `/api/refresh`** - 手动刷新所有订阅源
- 响应：`{ "success": true, "new_articles": 5 }`

**GET `/api/articles`** - 获取文章列表
- 查询参数：`tag`（可选）
- 响应：`{ "articles": [...] }`

---

## 4. 非功能性需求

### 4.1 性能

- **响应时间**：
  - 首页加载：< 2 秒
  - RSS 抓取：单个源 < 10 秒
  - AI 摘要生成：< 30 秒
- **并发能力**：支持 1 个用户（个人项目）
- **数据量**：支持 20 个订阅源，每天 50 篇新文章

### 4.2 安全

#### 4.2.1 凭据威胁模型

**威胁**：
- OpenAI API Key 泄露到 Git 仓库
- API Key 明文存储在配置文件中
- API Key 暴露在日志/终端输出中

**对策**：
1. **存储方案**：环境变量（`.env` 文件）
   - `.env` 文件加入 `.gitignore`
   - 提供 `.env.example` 模板
2. **录入方式**：
   - 首次运行：提示用户创建 `.env` 文件
   - 手动编辑 `.env` 文件，添加 `OPENAI_API_KEY=sk-...`
3. **使用方式**：
   - 通过 `python-dotenv` 加载环境变量
   - 代码中使用 `os.getenv("OPENAI_API_KEY")`
4. **更新/清除**：
   - 更新：编辑 `.env` 文件
   - 清除：删除 `.env` 文件中的对应行
5. **查看**：
   - 不提供查看功能（防止明文回显）
   - 只显示是否已配置（"已配置" / "未配置"）

**风险说明**：
- `.env` 文件为明文存储，进程环境变量可被同用户的其他进程读取
- 适合个人本地使用，不适合多用户环境
- 更安全的方案（操作系统钥匙串）可作为后期优化

#### 4.2.2 输入验证

- URL 验证：正则表达式 + 实际访问测试
- 标签验证：不允许特殊字符（`<`, `>`, `&`, `"`）
- SQL 注入防护：使用 ORM（SQLAlchemy）参数化查询

### 4.3 可用性

- **易用性**：
  - 界面简洁，核心功能一目了然
  - 添加订阅最多 3 步完成
  - 错误提示清晰友好
- **容错性**：
  - 单个订阅源失败不影响整体
  - LLM 调用失败有降级方案（显示原文）
- **可访问性**：
  - 语义化 HTML
  - 合理的颜色对比度
  - 支持键盘导航

### 4.4 可观测性

- **日志**：
  - 记录所有 RSS 抓取结果
  - 记录所有 LLM API 调用（成功/失败）
  - 记录所有错误和异常
  - 日志级别：DEBUG / INFO / WARNING / ERROR
  - 日志输出：控制台 + 文件（`logs/app.log`）
- **监控**：
  - 简单的健康检查接口：`GET /health`
  - 返回：`{ "status": "ok", "feeds": 10, "articles": 234 }`

---

## 5. 系统架构

### 5.1 架构图

```
┌─────────────────────────────────────────────────────┐
│                    用户浏览器                         │
│                   (Web Browser)                      │
└────────────────────┬────────────────────────────────┘
                     │ HTTP
                     ▼
┌─────────────────────────────────────────────────────┐
│                 Web 服务层 (FastAPI)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 首页路由 │  │ API路由  │  │ 订阅管理路由     │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
└────────┬────────────────┬────────────────┬─────────┘
         │                │                │
         ▼                ▼                ▼
┌─────────────────────────────────────────────────────┐
│                   业务逻辑层                         │
│  ┌──────────────┐  ┌─────────────┐  ┌──────────┐  │
│  │ 订阅管理服务 │  │ RSS抓取服务 │  │ 摘要服务 │  │
│  └──────────────┘  └─────────────┘  └──────────┘  │
└────────┬────────────────┬────────────────┬─────────┘
         │                │                │
         ▼                ▼                ▼
┌─────────────────────────────────────────────────────┐
│                   数据访问层 (ORM)                   │
│                   SQLAlchemy                         │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│                SQLite 数据库                         │
│           feeds 表  +  articles 表                   │
└─────────────────────────────────────────────────────┘

外部依赖：
┌──────────────┐       ┌──────────────────┐
│  RSS 源      │       │  OpenAI API      │
│  (外部网站)  │       │  (gpt-4o-mini)   │
└──────────────┘       └──────────────────┘
```

### 5.2 技术栈

| 层次 | 技术选择 | 说明 |
|------|---------|------|
| **后端框架** | FastAPI | 轻量级、高性能、自动生成 API 文档 |
| **RSS 解析** | feedparser | 成熟的 Python RSS 解析库 |
| **LLM 调用** | openai (Python SDK) | 官方 SDK，支持 gpt-4o-mini |
| **数据库** | SQLite | 轻量级、无需单独服务、适合单用户 |
| **ORM** | SQLAlchemy | Python 标准 ORM，类型安全 |
| **环境变量** | python-dotenv | 加载 `.env` 文件 |
| **HTTP 客户端** | httpx | 异步 HTTP 客户端，用于抓取 RSS |
| **定时任务** | 简单循环 + asyncio.sleep | 后台任务，无需额外依赖 |
| **前端** | Jinja2 模板 + HTML/CSS/JS | 服务端渲染，简单直接 |
| **日志** | Python logging | 标准库，无需额外依赖 |
| **部署** | Docker + Docker Compose | 容器化部署 |
| **云平台** | Render / Railway | 免费额度，支持 Docker |

### 5.3 目录结构

```
ai4se-final-project/
├── .env.example              # 环境变量模板
├── .gitignore                # Git 忽略文件
├── Dockerfile                # Docker 镜像构建
├── docker-compose.yml        # Docker Compose 配置
├── requirements.txt          # Python 依赖
├── README.md                 # 项目说明
├── SPEC.md                   # 本文件
├── PLAN.md                   # 实现计划（待生成）
├── CLAUDE.md                 # Superpowers 工作流指南
├── project-docs/             # 项目文档
│   ├── PROJECT_GUIDE_B.md
│   ├── PROJECT_PROGRESS.md
│   └── DELIVERABLES_CHECKLIST.md
├── src/                      # 源代码
│   ├── __init__.py
│   ├── main.py               # 应用入口
│   ├── config.py             # 配置管理
│   ├── database.py           # 数据库连接
│   ├── models.py             # 数据模型
│   ├── schemas.py            # Pydantic schemas
│   ├── services/             # 业务逻辑
│   │   ├── __init__.py
│   │   ├── feed_service.py   # 订阅管理
│   │   ├── rss_service.py    # RSS 抓取
│   │   └── summary_service.py # AI 摘要
│   ├── routers/              # 路由
│   │   ├── __init__.py
│   │   ├── web.py            # 网页路由
│   │   └── api.py            # API 路由
│   ├── templates/            # Jinja2 模板
│   │   ├── base.html
│   │   ├── index.html
│   │   └── feeds.html
│   └── static/               # 静态资源
│       ├── css/
│       │   └── style.css
│       └── js/
│           └── main.js
├── tests/                    # 测试代码
│   ├── __init__.py
│   ├── test_feed_service.py
│   ├── test_rss_service.py
│   └── test_summary_service.py
└── logs/                     # 日志目录
    └── app.log
```

---

## 6. 数据模型

### 6.1 数据库设计

#### Table: `feeds` (订阅源)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| name | VARCHAR(255) | NOT NULL | 订阅源名称 |
| url | VARCHAR(500) | NOT NULL, UNIQUE | RSS URL |
| tags | TEXT | | 标签（JSON 数组字符串） |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### Table: `articles` (文章)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| feed_id | INTEGER | NOT NULL, FOREIGN KEY(feeds.id) ON DELETE CASCADE | 订阅源 ID |
| title | VARCHAR(500) | NOT NULL | 文章标题 |
| link | VARCHAR(1000) | NOT NULL, UNIQUE | 原文链接 |
| content | TEXT | | 原文内容 |
| summary | TEXT | | AI 生成的摘要 |
| summary_status | VARCHAR(20) | NOT NULL, DEFAULT 'pending' | 摘要状态: pending/success/failed |
| published_at | DATETIME | NOT NULL | 发布时间 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 抓取时间 |

#### 索引设计

```sql
-- 加速按发布时间查询
CREATE INDEX idx_articles_published_at ON articles(published_at DESC);

-- 加速按订阅源查询
CREATE INDEX idx_articles_feed_id ON articles(feed_id);

-- 加速按摘要状态查询（用于批量生成摘要）
CREATE INDEX idx_articles_summary_status ON articles(summary_status);
```

### 6.2 数据示例

#### `feeds` 表示例

```json
{
  "id": 1,
  "name": "阮一峰的网络日志",
  "url": "https://www.ruanyifeng.com/blog/atom.xml",
  "tags": "[\"技术\", \"前端\"]",
  "created_at": "2026-08-13 10:00:00",
  "updated_at": "2026-08-13 10:00:00"
}
```

#### `articles` 表示例

```json
{
  "id": 1,
  "feed_id": 1,
  "title": "科技爱好者周刊（第 285 期）",
  "link": "https://www.ruanyifeng.com/blog/2026/08/weekly-issue-285.html",
  "content": "这里是本周的科技爱好者周刊...",
  "summary": "本周介绍了 3 个开源项目：一个轻量级的代码编辑器、一个 AI 写作助手...",
  "summary_status": "success",
  "published_at": "2026-08-13 08:00:00",
  "created_at": "2026-08-13 09:00:00"
}
```

---

## 7. 凭据与分发设计

### 7.1 凭据管理

#### 7.1.1 存储方案

**选择**：环境变量（`.env` 文件）

**原因**：
- 简单易用，适合个人项目
- 符合 12-Factor App 原则
- 易于在不同环境（开发/生产）切换

**实现**：
1. 项目根目录提供 `.env.example`：
   ```bash
   # OpenAI API Key
   OPENAI_API_KEY=your_api_key_here
   
   # 可选配置
   DATABASE_URL=sqlite:///./rss_aggregator.db
   LOG_LEVEL=INFO
   ```

2. `.gitignore` 包含 `.env`（防止提交）

3. 代码中使用 `python-dotenv` 加载：
   ```python
   from dotenv import load_dotenv
   import os
   
   load_dotenv()
   api_key = os.getenv("OPENAI_API_KEY")
   ```

#### 7.1.2 录入流程

**首次运行**：
1. 用户启动应用
2. 检测到 `OPENAI_API_KEY` 未配置
3. 显示提示：
   ```
   ⚠️  OpenAI API Key 未配置
   
   请按照以下步骤配置：
   1. 复制 .env.example 为 .env
   2. 编辑 .env 文件，填入你的 OpenAI API Key
   3. 重启应用
   
   获取 API Key: https://platform.openai.com/api-keys
   ```
4. 用户手动创建 `.env` 文件并填入 Key

**更新 Key**：
1. 编辑 `.env` 文件
2. 修改 `OPENAI_API_KEY` 的值
3. 重启应用

**清除 Key**：
1. 删除 `.env` 文件中的 `OPENAI_API_KEY` 行
2. 或删除整个 `.env` 文件

**查看状态**：
- 应用启动时在日志中显示：
  ```
  ✓ OpenAI API Key: 已配置
  ```
- **不回显明文**

#### 7.1.3 安全边界说明

**威胁模型**：
- ✅ 防护：Git 仓库泄露（`.env` 在 `.gitignore` 中）
- ✅ 防护：日志泄露（不记录 Key 值）
- ⚠️  有限防护：本地文件泄露（`.env` 为明文）
- ⚠️  有限防护：进程内存读取（环境变量可被同用户进程读取）

**适用场景**：
- ✅ 个人本地使用
- ✅ 单用户开发环境
- ⚠️  不适合多用户服务器
- ⚠️  不适合公共云环境（应使用密钥管理服务）

**风险披露**（在 README 中说明）：
> **安全说明**：本项目使用 `.env` 文件存储 API Key。该文件为明文存储，适合个人本地使用。如需更高安全性（多用户环境），请考虑使用操作系统钥匙串或云端密钥管理服务。

### 7.2 分发设计

#### 7.2.1 分发形态

**选择**：Docker 容器镜像

**原因**：
- 满足项目要求（必须提供容器或二进制分发）
- 跨平台（Linux/Mac/Windows）
- 依赖隔离（Python 环境、SQLite 等）
- 一致性（开发、测试、生产环境相同）

#### 7.2.2 Docker 镜像构建

**Dockerfile**：
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制源代码
COPY src/ ./src/
COPY templates/ ./templates/
COPY static/ ./static/

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**docker-compose.yml**：
```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data  # 持久化数据库
      - ./logs:/app/logs  # 持久化日志
    restart: unless-stopped
```

#### 7.2.3 分发流程

**1. 构建镜像**
```bash
docker build -t 7yashiki/rss-aggregator:latest .
```

**2. 推送到 Docker Hub**
```bash
docker login
docker push 7yashiki/rss-aggregator:latest
```

**3. 用户获取**
```bash
docker pull 7yashiki/rss-aggregator:latest
```

**4. 运行**
```bash
# 方式 1：docker run
docker run -d \
  -p 8000:8000 \
  -e OPENAI_API_KEY=sk-your-key \
  -v $(pwd)/data:/app/data \
  7yashiki/rss-aggregator:latest

# 方式 2：docker-compose
echo "OPENAI_API_KEY=sk-your-key" > .env
docker-compose up -d
```

#### 7.2.4 目标平台说明

| 平台 | 支持 | 说明 |
|------|------|------|
| **Linux** | ✅ | 主要测试平台 |
| **macOS** | ✅ | Docker Desktop |
| **Windows** | ✅ | Docker Desktop 或 WSL2 |
| **架构** | amd64 | 默认构建 |
| **依赖** | Docker 20.10+ | 需预先安装 Docker |

#### 7.2.5 Key 在目标机器的配置方式

**方式 1：环境变量（推荐用于 Docker）**
```bash
docker run -e OPENAI_API_KEY=sk-your-key ...
```

**方式 2：`.env` 文件（推荐用于 docker-compose）**
```bash
# 1. 创建 .env 文件
echo "OPENAI_API_KEY=sk-your-key" > .env

# 2. 启动
docker-compose up -d
```

**方式 3：交互式配置**
```bash
# 首次运行会提示创建 .env
docker-compose up
# 按照提示在宿主机创建 .env 文件，然后重启
```

#### 7.2.6 已知限制

- **网络依赖**：需要能访问外部 RSS 源和 OpenAI API
- **存储**：默认使用 SQLite，数据存储在容器内（需挂载 volume 持久化）
- **并发**：单实例，不支持多用户并发（按设计）
- **浏览器兼容性**：现代浏览器（Chrome 90+, Firefox 88+, Safari 14+）

---

## 8. 技术选型与理由

### 8.1 后端框架：FastAPI

**理由**：
1. **轻量高效**：适合小型 Web 应用
2. **异步支持**：原生支持 async/await，适合 I/O 密集型任务（RSS 抓取、LLM 调用）
3. **自动文档**：自动生成 OpenAPI 文档（`/docs`）
4. **类型安全**：基于 Pydantic，类型提示完善
5. **学习成本低**：文档清晰，社区活跃

**替代方案对比**：
- Flask：同步框架，异步支持不如 FastAPI
- Django：过于重量级，不适合小项目

### 8.2 LLM 供应商：OpenAI (gpt-4o-mini)

**理由**：
1. **性价比高**：$0.00015/1K input tokens，比 GPT-3.5-turbo 便宜
2. **质量好**：接近 GPT-4 水平
3. **API 稳定**：官方 SDK 成熟
4. **速度快**：响应时间 1-3 秒

**成本估算**（每天 50 篇文章，每篇 2000 字）：
- Input tokens: 50 × 2000 × 1.3 ≈ 130K tokens/天
- Output tokens: 50 × 200 × 1.3 ≈ 13K tokens/天
- 成本: 130K × $0.00015 + 13K × $0.0006 ≈ $0.027/天 ≈ $0.81/月

**替代方案对比**：
- Claude 3.5 Haiku：价格类似，但 API 配额可能更严格
- 本地模型（Llama）：免费但需要 GPU，部署复杂

### 8.3 数据库：SQLite

**理由**：
1. **零配置**：无需安装独立数据库服务
2. **轻量级**：单文件存储，易于备份
3. **够用**：单用户场景，性能足够（数千条记录）
4. **跨平台**：Python 标准库内置

**替代方案对比**：
- PostgreSQL：过于重量级，需要单独服务
- JSON 文件：查询性能差，不支持事务

### 8.4 前端：服务端渲染（Jinja2）

**理由**：
1. **简单直接**：无需前后端分离
2. **SEO 友好**：服务端渲染
3. **学习成本低**：无需学习 React/Vue
4. **够用**：个人项目，交互不复杂

**替代方案对比**：
- React/Vue SPA：过度设计，增加复杂度
- 纯静态 HTML：无法动态生成内容

### 8.5 部署平台：Render / Railway

**理由**：
1. **免费额度**：足够个人项目使用
2. **支持 Docker**：直接部署容器
3. **自动 HTTPS**：免费 SSL 证书
4. **易用性**：连接 GitHub 自动部署

**替代方案对比**：
- Heroku：免费额度已取消
- AWS/Azure：配置复杂，成本高
- Vercel：不支持长时间运行的后台任务

---

## 9. 验收标准

### 9.1 功能验收

| 功能 | 验收标准 | 测试方法 |
|------|---------|---------|
| **添加订阅** | 输入有效 RSS URL，成功添加到订阅列表 | 手动测试：添加阮一峰的博客 |
| **自动检测 RSS** | 输入网站 URL，自动发现并订阅 RSS | 手动测试：输入 `https://www.ruanyifeng.com/blog/` |
| **删除订阅** | 删除订阅源后，订阅列表中不再显示 | 手动测试 + 单元测试 |
| **标签管理** | 可以给订阅源添加/编辑标签 | 手动测试 |
| **抓取 RSS** | 点击刷新后，显示新文章 | 手动测试：对比刷新前后文章数量 |
| **生成摘要** | 文章列表显示 AI 生成的 100-200 字摘要 | 手动测试：检查摘要长度和质量 |
| **摘要失败处理** | LLM 调用失败时，显示"LLM 调用失败" + 原文前 200 字 | Mock OpenAI API 返回错误 |
| **按时间排序** | 文章按发布时间倒序显示 | 手动测试：检查列表顺序 |
| **按标签筛选** | 点击标签只显示该标签的文章 | 手动测试 |
| **跳转原文** | 点击标题在新标签页打开原文 | 手动测试 |
| **7天清理** | 超过 7 天的文章自动删除 | 单元测试：插入旧文章，运行清理，验证删除 |

### 9.2 非功能验收

| 项目 | 标准 | 测试方法 |
|------|------|---------|
| **响应时间** | 首页加载 < 2 秒 | 浏览器开发者工具 Network 面板 |
| **API Key 安全** | `.env` 在 `.gitignore` 中 | 检查 Git 历史，确保无 Key 泄露 |
| **Docker 分发** | 可一键启动：`docker-compose up` | 在全新机器测试 |
| **线上部署** | 提供公网可访问的 URL | 访问 Render/Railway 部署的应用 |
| **日志记录** | 所有错误写入日志文件 | 检查 `logs/app.log` |
| **错误处理** | 单个订阅源失败不影响其他 | 添加无效 RSS，验证其他订阅正常 |

### 9.3 完成定义

一个功能被认为"完成"需要满足：

1. ✅ **功能实现**：按 SPEC 要求实现
2. ✅ **测试通过**：单元测试覆盖核心逻辑
3. ✅ **代码评审**：通过两阶段评审（Spec 合规 + 代码质量）
4. ✅ **文档更新**：README 包含使用说明
5. ✅ **本地验证**：在本地环境手动测试通过
6. ✅ **Docker 验证**：在 Docker 环境测试通过

---

## 10. 风险与未决问题

### 10.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| **RSS 源失效** | 用户无法获取该源的内容 | 中 | 定期检查订阅源健康度，提示用户移除失效源 |
| **OpenAI API 限额** | 无法生成摘要 | 低 | 显示降级提示，使用原文前 200 字 |
| **OpenAI API 成本超预期** | 开发成本增加 | 低 | 控制摘要频率，使用缓存 |
| **RSS 格式不标准** | 解析失败 | 中 | feedparser 库容错性强，记录错误日志 |
| **网站反爬** | 无法抓取 RSS | 低 | RSS 是公开协议，通常不会被封 |
| **部署平台限制** | 免费额度不够 | 低 | Render 免费额度：750 小时/月，足够单实例 |

### 10.2 未决问题

1. **智能排序的具体实现**
   - **问题**：是否真的需要 AI 判断重要性？
   - **当前方案**：先按时间排序，后期再考虑
   - **决策时机**：MVP 完成后根据使用体验决定

2. **RSS 抓取频率**
   - **问题**：每小时抓取是否合适？
   - **当前方案**：每小时抓取一次
   - **可调整**：后期可根据订阅源活跃度动态调整

3. **摘要语言检测的准确性**
   - **问题**：如何准确检测文章语言？
   - **当前方案**：使用简单规则（检查中文字符占比）
   - **备选**：使用 `langdetect` 库

4. **移动端适配**
   - **问题**：是否需要响应式设计？
   - **当前方案**：简单的响应式（单列布局）
   - **决策**：MVP 阶段实现基础响应式

### 10.3 依赖风险

| 依赖 | 风险 | 缓解措施 |
|------|------|---------|
| **OpenAI API** | 服务中断、价格上涨 | 设计抽象层，便于切换到其他 LLM |
| **feedparser** | 库停止维护 | 核心依赖，社区活跃，风险低 |
| **SQLite** | 文件损坏 | 定期备份数据库文件 |
| **Render/Railway** | 免费额度取消 | 准备迁移到其他平台的方案 |

---

## 11. 附录

### 11.1 参考资源

- **RSS 规范**：https://www.rssboard.org/rss-specification
- **OpenAI API 文档**：https://platform.openai.com/docs/api-reference
- **FastAPI 文档**：https://fastapi.tiangolo.com/
- **feedparser 文档**：https://feedparser.readthedocs.io/
- **SQLAlchemy 文档**：https://docs.sqlalchemy.org/

### 11.2 术语表

| 术语 | 说明 |
|------|------|
| **RSS** | Really Simple Syndication，一种用于发布频繁更新数据的标准格式 |
| **Feed** | RSS 订阅源，一个 XML 文件 |
| **Entry** | RSS 中的一篇文章/条目 |
| **Aggregator** | 聚合器，收集多个 RSS 源的工具 |
| **LLM** | Large Language Model，大型语言模型 |
| **ORM** | Object-Relational Mapping，对象关系映射 |
| **TDD** | Test-Driven Development，测试驱动开发 |

### 11.3 示例 RSS 源（用于测试）

| 名称 | RSS URL | 说明 |
|------|---------|------|
| 阮一峰的网络日志 | https://www.ruanyifeng.com/blog/atom.xml | 技术博客，中文 |
| Hacker News | https://news.ycombinator.com/rss | 技术新闻，英文 |
| CSS-Tricks | https://css-tricks.com/feed/ | 前端技术，英文 |

---

## 变更历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| 1.0 | 2026-08-13 | 初始版本，完成 brainstorming | 7yaSHIKI + Claude Opus 5 |

---

**文档状态**：✅ 已完成，待评审

**下一步**：编写 `PLAN.md`（实现计划）
